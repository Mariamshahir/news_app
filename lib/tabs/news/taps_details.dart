import 'package:flutter/material.dart';

class TapsDetails extends StatelessWidget {
  const TapsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
